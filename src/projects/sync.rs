use anyhow::Result;

/// Pull latest changes for all projects on this machine.
pub fn pull_all(_paths: &[std::path::PathBuf]) -> Result<()> {
    // TODO: Фаза 3 — git pull для каждого репо
    Ok(())
}
